[build-system]
requires = ["setuptools>=69", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "femic-tsa29-workflows"
version = "0.1.0a1"
description = "TSA29-owned FEMIC workflow contracts"
readme = "README.md"
requires-python = ">=3.11"
license = "MIT"
authors = [
    {name = "UBC FRESH Lab"},
]
dependencies = [
    "PyYAML>=6",
]

[project.optional-dependencies]
dev = [
    "pytest",
    "ruff",
]

[project.entry-points."femic.named_pipeline_contracts"]
tsa29_locked_chain_strict = "tsa29_femic.locked_chain:provider_factory"

[project.entry-points."femic.tsr_adjudication_overlays"]
tsa29 = "tsa29_femic.tsr_adjudication:provider_factory"

[project.entry-points."femic.instance_catalogs"]
tsa29 = "tsa29_femic.instance_catalog:provider_factory"

[tool.setuptools]
package-dir = {"" = "src"}

[tool.setuptools.package-data]
tsa29_femic = ["resources/*.yaml"]

[tool.setuptools.packages.find]
where = ["src"]

[tool.pytest.ini_options]
testpaths = ["tests"]
