# blocks (externalized)

Patchworks block geometry and topology artifacts are externalized for thin-repo delivery.

Files validated in the source FEMIC workspace but not committed here:

- `blocks.shp`
- `blocks.dbf`
- `blocks.shx`
- `blocks.prj`
- `blocks.cpg`
- `topology_blocks_200r.csv`

Reference hashes are recorded in `metadata/large_artifacts.sha256`.
