# TSA29 validation artifact manifest

This directory carries compact validation bundle tables in-repo.

Large checkpoint artifacts (for example, full `checkpoint7-tsa29.feather`) are
externalized for thin-repo delivery and referenced via
`metadata/large_artifacts.sha256`.
